# Agent notes (Pantheon)

## Cursor Cloud specific instructions

- **THE ONE (canonical context):** The full exported Cursor thread that documents Path1 floor vs hybrid CPU deck, boot reveal / intro timing, GS `draw_finish` / wait ordering, Z-buffer alignment with libdraw samples, and the “clear + probe triangle only” debugging arc lives in **`docs/THE_ONE.md`**. Treat it as the **long-form incident log** when touching `floor.c`, boot flags, or Path1 vs GIF composition—prefer evidence there over re-deriving from memory.
- **Standard commands:** Build, profiles, and PCSX2 flow remain in **`README.md`**, **`GETTING_STARTED.md`**, and **`BETA_RELEASE.md`**; do not duplicate them here.
